<!-- res/drawable/search_bar_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Change the background color if needed -->
    <stroke android:width="1dp" android:color="@color/headingTextColor"/> <!-- Change the color to the desired border color -->
    <corners android:radius="15dp"/>
</shape>
